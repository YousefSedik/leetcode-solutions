            orig = ord(orig) - ord("a")        dist = [[INF ]* 26 for j in range(26)]        # add original => changed cost        for orig, chg, cst in zip(original, changed, cost):        INF = float("inf")        cost: List[int],    ) -> int:        original: List[str],        changed: List[str],        source: str,        target: str,class Solution:    def minimumCost(        self,